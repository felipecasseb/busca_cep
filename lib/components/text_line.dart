import 'package:flutter/material.dart';

class TextLine extends StatelessWidget {
  const TextLine({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
